# Server Config
server.port=${PORT:8082}

# Application Name
spring.application.name=complaint-system

# PostgreSQL Database Configuration (Port 5433 local, dynamic on Render)
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://dpg-dacn597qj5pc73dg9erg-a:5432/complientmanagement}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:complientmanagement_user}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:I3k2nDERTYwchSjakbHwKC61XFHm66uJ}
# Hikari Connection Pool Settings (Render Resiliency)
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.minimum-idle=1
spring.datasource.hikari.idle-timeout=30000
spring.datasource.hikari.max-lifetime=60000
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.initialization-fail-timeout=-1

# JPA / Hibernate Config (Optimized for Fast Cloud Startup)
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false
spring.jpa.properties.hibernate.temp.use_jdbc_metadata_defaults=false

# Multipart / File Upload Config
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=30MB
file.upload-dir=./uploads

# JWT Security Config
jwt.secret=9a4f2c8d7e1b3a5c7f9e2d4b6a8c0e1f3a5b7c9d1e3f5a7b9c1d3e5f7a9b1c3d
jwt.expiration-ms=86400000

# Email Configuration (JavaMailSender)
spring.mail.host=${SPRING_MAIL_HOST:smtp.gmail.com}
spring.mail.port=${SPRING_MAIL_PORT:587}
spring.mail.username=${SPRING_MAIL_USERNAME:eswarrawsr2006@gmail.com}
spring.mail.password=${SPRING_MAIL_PASSWORD:pqckzfpyntugeuoj}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.connectiontimeout=10000
spring.mail.properties.mail.smtp.timeout=10000
spring.mail.properties.mail.smtp.writetimeout=10000
app.mail.from=Campus Resolv Support <eswarrawsr2006@gmail.com>
